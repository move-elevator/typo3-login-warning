<f:layout name="SystemEmail" />
<f:section name="Subject">{prefix} Login at "{typo3.sitename}" from new IP address</f:section>
<f:section name="Title">{headline}</f:section>
<f:section name="Main">{introduction}
<f:variable name="locationInfo"><f:spaceless>
<f:if condition="{locationData} && {locationData.city} && {locationData.country}"><f:then>({locationData.city}, {locationData.country})</f:then></f:if></f:spaceless>
</f:variable>
<f:if condition="{isUserNotification}">
<f:then>You logged in from a new IP address "{normalizedParams.remoteAddress}"{locationInfo} at the site "{typo3.sitename}".
<f:if condition="{deviceInfo}">
(Device Info: <f:if condition="{deviceInfo.browser}">{deviceInfo.browser}</f:if><f:if condition="{deviceInfo.browser} && {deviceInfo.os}"> / </f:if><f:if condition="{deviceInfo.os}">{deviceInfo.os}</f:if><f:if condition="{deviceInfo.date}"> / {deviceInfo.date}</f:if>)
</f:if>

---

Was this you?
If you recognize this login, you can safely ignore this email. Your account remains secure.
If you did not perform this login, please contact your system administrator immediately and change your password.</f:then>
<f:else>The user "{user.username}" logged in from a new IP address "{normalizedParams.remoteAddress}"{locationInfo} at the site "{typo3.sitename}".
<f:if condition="{deviceInfo}">
(Device Info: <f:if condition="{deviceInfo.browser}">{deviceInfo.browser}</f:if><f:if condition="{deviceInfo.browser} && {deviceInfo.os}"> / </f:if><f:if condition="{deviceInfo.os}">{deviceInfo.os}</f:if><f:if condition="{deviceInfo.date}"> / {deviceInfo.date}</f:if>)
</f:if>

---

Security Notice:
This is an automated security notification. If this login was authorized, no action is required.
If this activity appears suspicious, please investigate the user account immediately.</f:else>
</f:if>
</f:section>
